# Web framework 
fastapi
uvicorn[standard]
python-multipart
slowapi
anthropic>=0.40.0

# Auth 
python-jose[cryptography]
PyJWT>=2.9.0
passlib[bcrypt]
bcrypt==4.0.1
authlib

# Database 
sqlalchemy[asyncio]
alembic
asyncpg
psycopg2-binary
aiosqlite

# Cache / task queue 
redis
celery
croniter

# Object storage : MinIO / S3-compatible 
aioboto3    
 
# RAG : embeddings + vector search 
voyageai          
pgvector      
 
# Real-time / SSE 
sse-starlette 
 
# Email notifications 
resend            # Resend API client — free tier 3,000 emails/month
 
# Observability 
structlog         # structured logging (JSON in prod, coloured in dev)
 
# HTTP client 
httpx>=0.27.0

# Settings 
pydantic
pydantic-settings
python-dotenv

# OCR engines
PyMuPDF
pytesseract
Pillow
PyPDF2
# Powers ocr_service.py's preprocessing pipeline (deskew, denoise, adaptive
# threshold) — without this, HAS_CV2 is False and every document silently
# falls back to the much weaker PIL-only path with no warning anywhere.
# -headless: no GUI bindings needed in a server/container environment.
opencv-python-headless

# Document export 
python-docx
reportlab
openpyxl

# Data 
numpy
pandas
unidecode

# Utilities 
aiofiles
email-validator
cryptography>=43.0.0

# NLP
spacy
